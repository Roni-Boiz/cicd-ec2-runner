name: Proxy Setup 

on:
  workflow_dispatch:

jobs:
  install-nginx:
    runs-on: git-workflow
    outputs:
      nginx_installed: ${{ steps.check-nginx.outputs.nginx_installed }}
    steps:
      - name: Check if Niginx is already installed
        id: check-nginx
        run: |
          if command -v nginx &> /dev/null; then
            echo "Nginx is already installed."
            echo "::set-output name=nginx_installed::true"
          else
            echo "::set-output name=nginx_installed::false"
          fi
        shell: bash

      - name: Prevent Re-run if Nginx is Installed
        if: steps.check-nginx.outputs.nginx_installed == 'true'
        run: exit 0

      - name: Install Nginx
        if: steps.check-nginx.outputs.nginx_installed == 'false'
        run: |
          sudo apt-get update
          sudo apt-get install nginx -y
          sudo ufw allow 'Nginx Full'
          sudo systemctl start nginx
          sudo systemctl enable nginx
          echo "nginx_installed=true" >> $GITHUB_ENV
          echo "Nginx installed successfully!"

  proxy-setup:
    runs-on: git-workflow
    needs: install-nginx
    if: needs.install-nginx.outputs.nginx_installed == 'true'
    steps:
      - name: Verify Nginx Installation
        run: nginx -v

      - name: Read Container ID
        run: |
          CONTAINER_IP=$(cat container_ip.txt)
          echo "CONTAINER_IP=$CONTAINER_IP" >> $GITHUB_ENV 
          echo "Container IP is $CONTAINER_IP"
        
      - name: Setup Proxy
        run: |
          sudo awk -v container_ip="$CONTAINER_IP" '
          /location \/ \{/ {
              if (!found) {
                  found = 1
                  print
                  print "                proxy_pass " container_ip
                  next
              }
          }
          {
              print
          }
          ' /etc/nginx/sites-available/default > temp_file && mv temp_file /etc/nginx/sites-available/default
          sudo systemctl reload nginx
